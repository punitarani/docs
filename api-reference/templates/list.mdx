---
title: "List templates"
openapi: "GET /v1/templates"
---

Retrieve a paginated list of all templates in your project, including both active and deprecated templates.

<Panel>
  <Tip>
    Filter out deprecated templates by checking the `deprecated_at` field is null.
  </Tip>
</Panel>

<RequestExample>

```bash cURL
curl -X GET "https://api.peepal.dev/v1/templates?limit=25" \
  -H "x-api-key: YOUR_API_KEY"
```

```python Python
import requests

url = "https://api.peepal.dev/v1/templates"
headers = {"x-api-key": "YOUR_API_KEY"}
params = {"limit": 25}

response = requests.get(url, headers=headers, params=params)
templates = response.json()

# Filter active templates
active = [t for t in templates["data"] if t["deprecated_at"] is None]
```

```javascript JavaScript
const url = new URL('https://api.peepal.dev/v1/templates');
url.searchParams.set('limit', '25');

const response = await fetch(url, {
  headers: {
    'x-api-key': 'YOUR_API_KEY'
  }
});

const templates = await response.json();
```

</RequestExample>

<ResponseExample>

```json 200 OK
{
  "data": [
    {
      "id": "tpl_01ARZ3NDEKTSV4RRFFQ69G5FAV",
      "name": "Employee Verification",
      "version": 1,
      "description": "Standard employee background check",
      "created_at": "2025-12-31T10:00:00Z",
      "deprecated_at": null,
      "input_schema": {},
      "extraction_schema": {},
      "search_plan": {},
      "scoring_rubric": {}
    }
  ],
  "next_cursor": null
}
```

</ResponseExample>
